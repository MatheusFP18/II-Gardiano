import Hero from "../components/Hero";
import MenuPreview from "../components/MenuPreview";

const Home = () => {
  return (
    <main>
      <Hero />
      <MenuPreview />
    </main>
  );
};

export default Home;
